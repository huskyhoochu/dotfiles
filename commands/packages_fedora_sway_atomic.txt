# Fedora Sway Atomic packages for rpm-ostree installation
# Lines starting with # are comments and will be ignored
# Empty lines will be skipped

# NOTE: Fedora Sway Atomic already includes many packages by default:
# - Core Sway ecosystem: sway, waybar, wl-clipboard, swaylock, swayidle, swaybg
# - Screenshot tools: grim, slurp
# - Media control: playerctl
# - Display management: kanshi, wlsunset, wdisplays
# - System tools: git, firefox, podman, buildah, NetworkManager
# - Terminal: foot (default)
# - File manager: Thunar
# - Image viewer: imv
# - Notification daemon: dunst (not mako)

# Core utilities (modern alternatives preferred)
cmatrix
fzf
btop
less
fastfetch
ripgrep
stow
tig
tmux
unzip
vim
zsh

# Development tools
neovim
lua
luarocks

# Wayland/Sway ecosystem - Additional packages not included by default
wofi
wlogout
pamixer
pavucontrol
brightnessctl
NetworkManager-tui

# Display manager (replaces SDDM)
greetd
greetd-tuigreet

# Korean input (fcitx5)
fcitx5
fcitx5-hangul
fcitx5-configtool
fcitx5-gtk
fcitx5-qt

# Fonts
google-noto-sans-cjk-fonts
google-noto-serif-cjk-fonts
google-noto-color-emoji-fonts

# Media
mupdf

# COPR packages (require COPR activation before installation)
# Activate COPR: sudo dnf copr enable <user>/<repo>
# Install: rpm-ostree install <package> && systemctl reboot
# Use setup_swaylock.sh script for automatic installation

# swaylock-effects - Enhanced screen locker with effects
# COPR: eddsalkield/swaylock-effects
# Script: ./setup_swaylock.sh

# Manual installation required (not in default repos):
# - ghostty (build from source or COPR)
# - lazygit (COPR: sudo dnf copr enable atim/lazygit && rpm-ostree install lazygit)
# - oh-my-posh (COPR: sudo dnf copr enable chronoscrat/oh-my-posh && rpm-ostree install oh-my-posh)
# - spicetify-cli (npm install -g spicetify-cli or manual)
# - dropbox (download RPM from dropbox.com)
# - 1password (download RPM from 1password.com)
# - obsidian (Flatpak recommended: flatpak install flathub md.obsidian.Obsidian)
# - spotify (Flatpak recommended: flatpak install flathub com.spotify.Client)
# - nerd-fonts (individual packages or manual install to ~/.local/share/fonts/)

# 매시 백업 타이머 (호스트 /etc/systemd/system/)
# Persistent: 꺼져 있던 시간대의 회차를 부팅 직후 따라잡는다.
# RandomizedDelaySec: 정각에 몰리는 다른 타이머들과의 경합을 피한다.

[Unit]
Description=Hourly backup (btrfs snapshot + restic)

[Timer]
OnCalendar=hourly
RandomizedDelaySec=120
Persistent=true

[Install]
WantedBy=timers.target

# 5분 주기 상태 점검 타이머 (호스트 /etc/systemd/system/)
# Kuma push 모니터의 하트비트 주기(300초)와 맞춘다. Persistent 는 두지 않는다 —
# 밀린 점검을 따라잡을 이유가 없다.

[Unit]
Description=Health check every 5 minutes

[Timer]
OnCalendar=*:0/5
RandomizedDelaySec=30

[Install]
WantedBy=timers.target

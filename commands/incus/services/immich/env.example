# /etc/immich.env — media 컨테이너 안, root:root 600
#
# DB_PASSWORD / POSTGRES_PASSWORD  op://Personal/GEM12_IMMICH_DB — 같은 값.
#   영숫자만 쓴다 — 특수문자는 Immich 접속 문자열에서 문제를 일으킨다 (공식 권고).
#   server 가 DB_PASSWORD 로 접속하고 postgres 가 POSTGRES_PASSWORD 로 초기화한다.

DB_PASSWORD=
POSTGRES_PASSWORD=

# Fedora 44 Workstation packages
# Sections:
#   dnf:   Fedora official repos (dnf install)
#   brew:  Homebrew on Linux (brew install)
#   repo:  extra repositories required — line format: <repo-spec> <package>
#          copr:<user>/<project> <package>   → dnf copr enable
#          <url-to-.repo-file> <package>     → dnf config-manager addrepo
# Lines starting with # and blank lines are skipped.

dnf:

# Core utilities (modern alternatives preferred)
cmatrix
eza
fzf
git
btop
less
fastfetch
ripgrep
stow
tig
tmux
unzip
vim
zsh

# Development tools
neovim
lua
luarocks

# Prompt theme (official repo since Fedora 44 — COPR no longer needed)
oh-my-posh

# Korean input (fcitx5)
fcitx5
fcitx5-hangul
fcitx5-configtool
fcitx5-gtk
fcitx5-qt

# Fonts
google-noto-sans-cjk-fonts
google-noto-serif-cjk-fonts
google-noto-color-emoji-fonts

# Browsers
firefox

# Media
mupdf

brew:

# Not in Fedora official repos; Homebrew ships Linux bottles for these
lazygit
spicetify-cli

repo:

# Ghostty terminal — community COPR (not in official repos as of Fedora 44)
copr:scottames/ghostty ghostty

# Vivaldi browser — vendor repo (package name is vivaldi-stable, not vivaldi)
https://repo.vivaldi.com/stable/vivaldi-fedora.repo vivaldi-stable

# Manual installation required (no repo / GUI apps):
# - 1password (download RPM from 1password.com, or their yum repo)
# - dropbox (download RPM from dropbox.com)
# - obsidian (Flatpak: md.obsidian.Obsidian)
# - spotify (Flatpak: com.spotify.Client)
# - nerd-fonts (download to ~/.local/share/fonts/, then fc-cache -fv)
